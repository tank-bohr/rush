module Rush
  module AST
    # Base for every AST node. Subclasses implement #execute(executor) so the
    # executor dispatches polymorphically and never switches on node type.
    class Node
      def execute: (untyped _executor) -> untyped
    end
  end
end
