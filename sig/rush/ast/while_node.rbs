module Rush
  module AST
    # `while cond; do body; done` — runs the body while the condition succeeds.
    class While < ConditionLoop
      extend T::Sig

      private

      def kind: () -> :while
    end
  end
end
