# Project Instructions for AI Agents

**Read [AGENTS.md](AGENTS.md) now** — it is the canonical, full set of project instructions
(build & test, slice workflow, commit/push policy, where knowledge lives, beads). This file is
only a pointer so Claude Code's native loader picks them up too.
